import {
  Home,
  FirstName,
  LastName,
  Industry,
  Role,
  ProfessionalGoal,
  EmailAddress,
  PhoneNumber,
  Success,
} from '../components';

const componentArray = [
  Home,
  FirstName,
  LastName,
  Industry,
  Role,
  ProfessionalGoal,
  EmailAddress,
  PhoneNumber,
  Success,
];

const formElements = {
  componentArray,
  numberOfElements: componentArray.length,
};

export default formElements;
